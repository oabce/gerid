version: '3.8'

services:
  app:
    build: .
    container_name: portal-interno
    restart: unless-stopped
    ports:
      - "3002:3002"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3002
